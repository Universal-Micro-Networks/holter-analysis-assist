# tools/

BeatSense reference（Python）と ONNX エクスポート用ツールです。

| パス | 内容 |
|---|---|
| `beatsense/` | Phase-2 `build_model` / 前処理 / 後処理（外部実装向け reference） |
| `beatsense/REFERENCE.md` | BeatSense README（仕様正本） |
| `export/` | H5 → ONNX 変換スクリプト |

Rust ランタイムはリポジトリ直下の `src/` を参照してください。
